[build]
builder = "nixpacks"
buildCommand = "npm run build:ncaa"

[deploy]
startCommand = "DB_PATH=/data/calcutta.db node apps/ncaa/server/index.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
