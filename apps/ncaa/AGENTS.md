# NCAA AGENTS Overlay

This file augments root `AGENTS.md` for NCAA-scoped work.

## Scope

Apply these rules when editing `apps/ncaa/**`.

## Additional Guardrails

1. Preserve NCAA UX language and styling unless task explicitly requests UI redesign.
2. Keep auction/bracket/payout logic deterministic.
3. Validate changes with `npm run test:ncaa`.
4. If deployment behavior changes, update root `RUNBOOK.md` and `HEARTBEAT.md`.
